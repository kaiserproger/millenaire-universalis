name: ci

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - run: sudo apt-get update -qq && sudo apt-get install -y -qq jq ripgrep shellcheck
      - run: |
          bash -n qa/full-cycle/bin/run.sh qa/full-cycle/bin/static-audit.sh
          shellcheck -e SC2086 -e SC2155 qa/full-cycle/bin/run.sh qa/full-cycle/bin/static-audit.sh
          qa/full-cycle/bin/static-audit.sh
      - uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            vendor
          key: millenaire-universalis-${{ runner.os }}-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties', 'gradle.properties', 'scripts/materialize-dependencies.py') }}
      - run: python3 scripts/materialize-dependencies.py
      - run: ./gradlew --no-daemon --no-parallel --no-configuration-cache clean check build
      - uses: actions/upload-artifact@v4
        with:
          name: millenaire-universalis
          path: build/libs/millenaire_universalis-*.jar

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: millenaire-universalis
          path: release
      - env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create -R "$GITHUB_REPOSITORY" "$GITHUB_REF_NAME" release/*.jar --verify-tag --generate-notes

